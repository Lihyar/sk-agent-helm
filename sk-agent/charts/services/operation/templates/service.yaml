apiVersion: v1
kind: Service
metadata:
  name: {{ include "operation.fullname" . }}
  labels:
    {{- include "operation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "operation.selectorLabels" . | nindent 4 }}