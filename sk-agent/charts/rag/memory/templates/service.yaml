apiVersion: v1
kind: Service
metadata:
  name: {{ include "memory.fullname" . }}
  labels:
    {{- include "memory.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "memory.selectorLabels" . | nindent 4 }}