apiVersion: v1
kind: Service
metadata:
  name: {{ include "hospital-web.fullname" . }}
  labels:
    {{- include "hospital-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hospital-web.selectorLabels" . | nindent 4 }}
