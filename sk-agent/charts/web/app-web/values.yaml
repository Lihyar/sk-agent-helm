replicaCount: 1

image:
  repository: registry.example.com/sk-agent/app-web
  tag: latest
  pullPolicy: IfNotPresent

port: 3000

service:
  type: ClusterIP
  port: 80

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi

healthEndpoint: /health
